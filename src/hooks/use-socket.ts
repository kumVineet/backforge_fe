// Backward compatibility hook - now uses the socket context
export { useSocket, useSocketConnection } from '@/contexts';

// Re-export types for convenience
export type { SocketState, SocketActions, SocketContextValue } from '@/contexts';
